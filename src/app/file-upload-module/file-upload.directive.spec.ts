import { FileUploadDirectiveDirective } from './file-upload-directive.directive';

describe('FileUploadDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FileUploadDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
